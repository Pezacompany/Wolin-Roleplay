{
  "name": "discord-roblox-verification-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "npm --prefix bot run check && npm --prefix strona run check"
  },
  "workspaces": [
    "bot",
    "strona"
  ]
}

